/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["public.flourish.studio"], // Keep if you're using external images
  },
};

export default nextConfig;
